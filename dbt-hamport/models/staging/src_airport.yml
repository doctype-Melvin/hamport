version: 2

sources:
  - name: raw_data
    schema: raw
    tables:
      - name: raw_arrivals
        columns:
          - name: flightnumber
            tests:
              - not_null
      - name: raw_departures
        columns:
          - name: flightnumber
            tests:
              - not_null
      - name: raw_weather
        columns:
          - name: time
            tests:
              - not_null